import styled from "styled-components";

export const AvatarCardStyle = styled.div`
  .hero-image {
    height: 50%;
    background-position: center;
    background-repeat: no-repeat;
    background-size: cover;
    position: relative;
  }

  .hero-text {
    position: absolute;

    color: white;
  }
  h1 {
    font-size: 60px;
  }
  p {
    font-size: 20px;
  }
`;
