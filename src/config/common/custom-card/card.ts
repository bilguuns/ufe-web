import styled from "styled-components";

export const CardStyled = styled.div`
    .card {
        word-wrap: break-word;
        overflow: hidden;
        border: none;
        border-top: 5px solid #2d94ce !important;
        border-radius: 0 0 5px 5px;
        box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.15);
    }

    .ant-card-head-title {
        white-space: normal;
    }
`;
